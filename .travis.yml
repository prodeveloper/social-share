language: php

sudo: false

php: 
  - 5.6
  - 7.0
  - hhvm
  - nightly

install:
  composer install

cache:
  directories:
    - vendor
    # - vendor/bundle
    # - $HOME/.cache/bower
    # - $HOME/.cache/pip
    # - $HOME/.ccache
    - $HOME/.composer/cache
    # - $HOME/.npm
    # - $HOME/.nvm
